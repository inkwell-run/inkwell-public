import React from "react";
import logo from "../assets/logo.svg";

const Header = () => {
  return (
    <img
      src={logo}
      alt=""
      style={{
        height: "1.2rem",
      }}
    />
  );
};

export default Header;
